# Feature spec template

Copy this into `docs/features/<feature-name>.md` for any UI/product feature (frontend
components, flows) that isn't itself a program milestone — those go in `docs/product/` instead.

---

# {{Feature name}}

**Status:** prototype | in progress | shipped
**Owner milestone:** {{link to the `docs/product/` milestone this feature belongs to, if any}}

## Problem

What the user can't do today, or what's missing.

## Solution

What this feature does, in plain terms.

## Scope

- In:
- Out (explicitly deferred, and to where):

## Implementation notes

Where the code lives, what it depends on (design system, SDK, wallet adapter, etc.), what's
stubbed vs. real.

## Verification

How this was checked — manual pass, screenshot, automated test. Be explicit about what was
*not* verified.
